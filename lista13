#include <stdio.h>

int main() {
    int num;

    printf("Digite um número: ");
    scanf("%d", &num);

    if (num % 5 == 0) {
        printf("O número %d é múltiplo de 5.\n", num);
    } else {
        printf("O número %d não é múltiplo de 5.\n", num);
    }

    return 0;
}